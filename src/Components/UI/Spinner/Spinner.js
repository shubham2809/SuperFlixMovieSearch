import React from 'react';
import './Spinner.css';

//Spinner is a stateless component 
// Stateless component doesnt maintains the state and improves performance
const Spinner = () => (
    <div className="loader">Loading...</div>
);

export default Spinner;